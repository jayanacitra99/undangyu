// Breeze's Blade components (nav dropdown, responsive menu, modals) are Alpine-driven.
// AdminLTE + Vue islands replace this shell in Session 3.
import Alpine from 'alpinejs';

window.Alpine = Alpine;

Alpine.start();
